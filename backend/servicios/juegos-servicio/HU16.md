# HU16 — Agregar Pregunta a Trivia

**Como** Operador, **quiero** agregar preguntas de selección simple o múltiple a una trivia en estado de borrador, **para** definir el contenido antes de activarla.

---

## Criterios de aceptación

- Solo se pueden agregar preguntas a trivias en estado **Borrador**.
- Cada pregunta debe tener al menos **2 opciones**.
- Al menos **una opción** debe estar marcada como correcta.
- El puntaje asignado debe ser **mayor a cero**.
- El operador debe ser dueño de la trivia (validado vía token JWT).

---

## Flujo

```
Frontend → POST /api/juegos/trivias/{triviaId}/preguntas
           Authorization: Bearer <token-operador>
           { enunciado, puntajeAsignado, opciones: [{ texto, esCorrecta }] }
        ↓
TriviasControlador.AgregarPregunta(triviaId, AgregarPreguntaDto)
        ↓
AgregarPreguntaComando(triviaId, dto) → MediatR
        ↓
AgregarPreguntaManejador:
  1. repositorio.ObtenerTriviaPorIdAsync(triviaId)
       → si no existe → ExcepcionNoEncontrado (404)
  2. trivia.AgregarPregunta(enunciado, puntaje, opciones)
       → ValidarEstadoBorrador()    ← si Activa → ExcepcionDominio (422)
       → Pregunta.Crear(...)        ← valida ≥2 opciones, ≥1 correcta, puntaje > 0
       → agrega Pregunta a _preguntas
  3. repositorio.AgregarPreguntaAsync(triviaId, pregunta)
        ↓
HTTP 201 Created  { id: "guid-de-la-pregunta" }
```

---

## Endpoint

| Método | Ruta | Descripción |
|--------|------|-------------|
| `POST` | `/api/juegos/trivias/{triviaId}/preguntas` | Agregar pregunta a trivia en borrador |

---

## Archivos creados / modificados

### Nuevos
- `JuegosServicio.Commons/Dtos/AgregarPreguntaDto.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Comandos/AgregarPreguntaComando.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Manejadores/AgregarPreguntaManejador.cs`
- `frontend-web/.../paginas/PaginaGestionPreguntas.tsx`

### Modificados
- `JuegosServicio.Aplicacion/Puertos/IRepositorioJuegos.cs` — agrega `AgregarPreguntaAsync`
- `JuegosServicio.Infraestructura/Persistencia/RepositorioJuegos.cs` — implementa `AgregarPreguntaAsync`
- `JuegosServicio.Presentacion/Controladores/TriviasControlador.cs` — agrega endpoint `POST .../preguntas`

---

## Cuerpo de ejemplo

```json
{
  "enunciado": "¿Cuál es la capital de Venezuela?",
  "puntajeAsignado": 10,
  "opciones": [
    { "texto": "Caracas",   "esCorrecta": true  },
    { "texto": "Maracaibo", "esCorrecta": false },
    { "texto": "Valencia",  "esCorrecta": false }
  ]
}
```

**Respuesta:**
```json
{ "id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" }
```
