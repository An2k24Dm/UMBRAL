# HU17 — Modificar / Eliminar Pregunta

**Como** Operador, **quiero** editar o eliminar una pregunta específica de una trivia en borrador, **para** corregir errores antes de activarla.

---

## Criterios de aceptación

- Solo se pueden modificar o eliminar preguntas de trivias en estado **Borrador**.
- La pregunta a modificar/eliminar debe existir en la trivia indicada.
- Al modificar: las opciones se reemplazan completamente (no se actualiza parcialmente).
- Al eliminar: se eliminan en cascada las opciones de esa pregunta.

---

## Flujo — Modificar

```
Frontend → PUT /api/juegos/trivias/{triviaId}/preguntas/{preguntaId}
           Authorization: Bearer <token-operador>
           { nuevoEnunciado, nuevasOpciones: [{ texto, esCorrecta }] }
        ↓
ModificarPreguntaComando(triviaId, preguntaId, dto) → MediatR
        ↓
ModificarPreguntaManejador:
  1. repositorio.ObtenerTriviaPorIdAsync(triviaId)
       → si no existe → ExcepcionNoEncontrado (404)
  2. trivia.ModificarPregunta(preguntaId, nuevoEnunciado, nuevasOpciones)
       → ValidarEstadoBorrador()
       → busca Pregunta por ID → si no existe → ExcepcionNoEncontrado (404)
       → pregunta.Modificar(...) ← borra opciones y crea nuevas
  3. repositorio.ModificarPreguntaAsync(triviaId, preguntaModificada)
       → DELETE opciones anteriores + INSERT nuevas
       → UPDATE enunciado en PreguntaModelo
        ↓
HTTP 204 No Content
```

## Flujo — Eliminar

```
Frontend → DELETE /api/juegos/trivias/{triviaId}/preguntas/{preguntaId}
           Authorization: Bearer <token-operador>
        ↓
EliminarPreguntaComando(triviaId, preguntaId) → MediatR
        ↓
EliminarPreguntaManejador:
  1. repositorio.ObtenerTriviaPorIdAsync(triviaId)
       → si no existe → ExcepcionNoEncontrado (404)
  2. trivia.EliminarPregunta(preguntaId)
       → ValidarEstadoBorrador()
       → busca Pregunta por ID → si no existe → ExcepcionNoEncontrado (404)
       → remueve de _preguntas
  3. repositorio.EliminarPreguntaAsync(triviaId, preguntaId)
       → DELETE PreguntaModelo (CASCADE elimina OpcionModelo)
        ↓
HTTP 204 No Content
```

---

## Endpoints

| Método   | Ruta | Descripción |
|----------|------|-------------|
| `PUT`    | `/api/juegos/trivias/{triviaId}/preguntas/{preguntaId}` | Modificar pregunta existente |
| `DELETE` | `/api/juegos/trivias/{triviaId}/preguntas/{preguntaId}` | Eliminar pregunta |

---

## Archivos creados / modificados

### Nuevos
- `JuegosServicio.Commons/Dtos/ModificarPreguntaDto.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Comandos/ModificarPreguntaComando.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Comandos/EliminarPreguntaComando.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Manejadores/ModificarPreguntaManejador.cs`
- `JuegosServicio.Aplicacion/CasosDeUso/Manejadores/EliminarPreguntaManejador.cs`

### Modificados
- `JuegosServicio.Aplicacion/Puertos/IRepositorioJuegos.cs` — agrega `ModificarPreguntaAsync` y `EliminarPreguntaAsync`
- `JuegosServicio.Infraestructura/Persistencia/RepositorioJuegos.cs` — implementa ambos métodos
- `JuegosServicio.Presentacion/Controladores/TriviasControlador.cs` — agrega endpoints `PUT` y `DELETE`

---

## Cuerpo de ejemplo — Modificar

```json
{
  "nuevoEnunciado": "¿Capital de Venezuela?",
  "nuevasOpciones": [
    { "texto": "Caracas",  "esCorrecta": true  },
    { "texto": "Lima",     "esCorrecta": false },
    { "texto": "Bogotá",   "esCorrecta": false },
    { "texto": "La Paz",   "esCorrecta": false }
  ]
}
```
